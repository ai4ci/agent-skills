as.list(implementation[1,])
implementation = current_files %>%
dplyr::filter(!(
startsWith(path, "design") |
startsWith(path, "architecture")
) & fs::path_dir(path) != ".") %>%
dplyr::anti_join(
current_links %>% dplyr::filter(type %in% c("TESTDATA")),
by = c("path" = "target")
)
new_implementation_notes = implementation %>% dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ grepv("(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ length(.x) / .y * 100)
) %>% dplyr::select(path,notes,total,percent)
new_implementation_notes = implementation %>% dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ grepv("(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ length(.x) / .y * 100)
) %>% dplyr::select(path,notes,total,percent)
tmp = lapply(nrow(new_implementation_notes), function(i) {
ls = as.list(new_implementation_notes[i,])
})
names(tmp) = new_implementation_notes$path
new_implementation_notes$path
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
ls = as.list(new_implementation_notes[i,])
})
names(tmp) = new_implementation_notes$path
tmp
.tree(tmp)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i,]
ls = as.list(row %>% dplyr::select(-path))
})
names(tmp) = new_implementation_notes$path
implementation_notes = modifyList(implementation_notes, tmp)
implementation_notes_file = fs::path(em_dir, "implementation-notes.json")
if (fs::exists(implementation_notes_file)) {
implementation_notes = jsonlite::read_json(implementation_notes_file)
} else {
implementation_notes = list()
}
implementation_notes = list()
if (fs::file_exists(implementation_notes_file)) {
implementation_notes = jsonlite::read_json(implementation_notes_file)
} else {
implementation_notes = list()
}
new_implementation_notes = implementation %>% dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ grepv("(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ length(.x) / .y * 100)
) %>% dplyr::select(path,notes,total,percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i,]
ls = as.list(row %>% dplyr::select(-path))
})
names(tmp) = new_implementation_notes$path
implementation_notes = modifyList(implementation_notes, tmp)
jsonlite::write_json(implementation_notes, implementation_notes_file)
jsonlite::write_json(implementation_notes, implementation_notes_file, prettify=TRUE)
?jsonlite::write_json
jsonlite::write_json(implementation_notes, implementation_notes_file, pretty=TRUE, simplifyVector = TRUE)
jsonlite::write_json(implementation_notes, implementation_notes_file, pretty=TRUE, auto_unbox=TRUE)
?unlist
implementation_notes = list()
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i,]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive=FALSE)
})
names(tmp) = new_implementation_notes$path
implementation_notes = modifyList(implementation_notes, tmp)
implementation_notes
jsonlite::write_json(implementation_notes, implementation_notes_file, pretty=TRUE, auto_unbox=TRUE)
implementation_notes = list()
new_implementation_notes = implementation %>% dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ grepv("(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ length(.x) / .y * 100)
) %>% dplyr::select(path,timestamp,notes,total,percent)
jsonlite::write_json(implementation_notes, implementation_notes_file, pretty=TRUE, auto_unbox=TRUE)
new_implementation_notes = implementation %>% dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ grepv("(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ length(.x) / .y * 100)
) %>% dplyr::select(path,modified,notes,total,percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i,]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive=FALSE)
})
names(tmp) = new_implementation_notes$path
implementation_notes = modifyList(implementation_notes, tmp)
jsonlite::write_json(implementation_notes, implementation_notes_file, pretty=TRUE, auto_unbox=TRUE)
implementation_notes = implementation_notes[order(names(implementation_notes))]
jsonlite::write_json(implementation_notes, implementation_notes_file, pretty=TRUE, auto_unbox=TRUE)
names(implementation_notes)
order(names(implementation_notes))
new_implementation_notes = implementation %>% dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ dplyr::tibble(
line_no = which(grepl("(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)),
comment = grepv("(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)
)),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ length(.x) / .y * 100)
) %>% dplyr::select(path,modified,notes,total,percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i,]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive=FALSE)
})
tmp
names(tmp) = new_implementation_notes$path
implementation_notes = modifyList(implementation_notes, tmp)
implementation_notes = implementation_notes[order(names(implementation_notes))]
jsonlite::write_json(implementation_notes, implementation_notes_file, pretty=TRUE, auto_unbox=TRUE)
implementation_notes = list()
new_implementation_notes = implementation %>% dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ dplyr::tibble(
line_no = which(grepl("(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)),
comment = grepv("(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)
)),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ nrow(.x) / .y * 100)
) %>% dplyr::select(path,modified,notes,total,percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i,]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive=FALSE)
})
names(tmp) = new_implementation_notes$path
current_implementation_notes = modifyList(implementation_notes, tmp)
current_implementation_notes = current_implementation_notes[order(names(current_implementation_notes))]
jsonlite::write_json(current_implementation_notes, implementation_notes_file, pretty=TRUE, auto_unbox=TRUE)
implementation_notes = list()
new_implementation_notes = implementation %>% dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ dplyr::tibble(
line_no = which(grepl("(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)),
comment = grepv("(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)
)),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ nrow(.x) / .y * 100)
) %>% dplyr::select(path,modified,notes,total,percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i,]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive=FALSE)
})
names(tmp) = new_implementation_notes$path
current_implementation_notes = modifyList(implementation_notes, tmp)
current_implementation_notes = current_implementation_notes[order(names(current_implementation_notes))]
jsonlite::write_json(current_implementation_notes, implementation_notes_file, pretty=TRUE, auto_unbox=TRUE)
new_implementation_notes = implementation %>%
dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ dplyr::tibble(
line_no = which(grepl("\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)),
comment = grepv("\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)
)),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ nrow(.x) / .y * 100)
) %>%
dplyr::select(path, modified, notes, total, percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i, ]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive = FALSE)
})
names(tmp) = new_implementation_notes$path
current_implementation_notes = modifyList(implementation_notes, tmp)
current_implementation_notes = current_implementation_notes[order(names(current_implementation_notes))]
jsonlite::write_json(
current_implementation_notes,
implementation_notes_file,
pretty = TRUE,
auto_unbox = TRUE
)
new_implementation_notes = implementation %>%
dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ dplyr::tibble(
line_no = which(grepl("$\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)),
comment = grepv("$\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)
)),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ nrow(.x) / .y * 100)
) %>%
dplyr::select(path, modified, notes, total, percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i, ]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive = FALSE)
})
names(tmp) = new_implementation_notes$path
current_implementation_notes = modifyList(implementation_notes, tmp)
current_implementation_notes = current_implementation_notes[order(names(current_implementation_notes))]
jsonlite::write_json(
current_implementation_notes,
implementation_notes_file,
pretty = TRUE,
auto_unbox = TRUE
)
new_implementation_notes = implementation %>%
dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ dplyr::tibble(
line_no = which(grepl("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)),
comment = grepv("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)
)),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ nrow(.x) / .y * 100)
) %>%
dplyr::select(path, modified, notes, total, percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i, ]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive = FALSE)
})
names(tmp) = new_implementation_notes$path
current_implementation_notes = modifyList(implementation_notes, tmp)
current_implementation_notes = current_implementation_notes[order(names(current_implementation_notes))]
jsonlite::write_json(
current_implementation_notes,
implementation_notes_file,
pretty = TRUE,
auto_unbox = TRUE
)
new_implementation_notes = implementation %>%
dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ {
comment = grepv("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)
line_no = which(grepl("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x))
names(comment) = sprintf("line: %d",line_no)
}),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ nrow(.x) / .y * 100)
) %>%
dplyr::select(path, modified, notes, total, percent)
new_implementation_notes = implementation %>%
dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ {
comment = grepv("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)
line_no = which(grepl("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x))
names(comment) = sprintf("line: %d",line_no)
list(comment)
}),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ length(.x) / .y * 100)
) %>%
dplyr::select(path, modified, notes, total, percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i, ]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive = FALSE)
})
names(tmp) = new_implementation_notes$path
current_implementation_notes = modifyList(implementation_notes, tmp)
current_implementation_notes = current_implementation_notes[order(names(current_implementation_notes))]
jsonlite::write_json(
current_implementation_notes,
implementation_notes_file,
pretty = TRUE,
auto_unbox = TRUE
)
new_implementation_notes = implementation %>%
dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ {
comment = grepv("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x)
line_no = which(grepl("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x))
names(comment) = sprintf("line: %d",line_no)
comment
}),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ length(.x) / .y * 100)
) %>%
dplyr::select(path, modified, notes, total, percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i, ]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive = FALSE)
})
names(tmp) = new_implementation_notes$path
current_implementation_notes = modifyList(implementation_notes, tmp)
current_implementation_notes = current_implementation_notes[order(names(current_implementation_notes))]
jsonlite::write_json(
current_implementation_notes,
implementation_notes_file,
pretty = TRUE,
auto_unbox = TRUE
)
new_implementation_notes = implementation %>%
dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ {
comment = as.list(grepv("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x))
line_no = which(grepl("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x))
names(comment) = sprintf("line: %d",line_no)
comment
}),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ length(.x) / .y * 100)
) %>%
dplyr::select(path, modified, notes, total, percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i, ]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive = FALSE)
})
names(tmp) = new_implementation_notes$path
current_implementation_notes = modifyList(implementation_notes, tmp)
current_implementation_notes = current_implementation_notes[order(names(current_implementation_notes))]
jsonlite::write_json(
current_implementation_notes,
implementation_notes_file,
pretty = TRUE,
auto_unbox = TRUE
)
new_implementation_notes = implementation %>%
dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
notes = purrr::map(lines, ~ {
line_no = which(grepl("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x))
comment = as.list(.x[line_no])
names(comment) = sprintf("line: %d",line_no)
comment
}),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ length(.x) / .y * 100)
) %>%
dplyr::select(path, modified, notes, total, percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i, ]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive = FALSE)
})
names(tmp) = new_implementation_notes$path
current_implementation_notes = modifyList(implementation_notes, tmp)
current_implementation_notes = current_implementation_notes[order(names(current_implementation_notes))]
jsonlite::write_json(
current_implementation_notes,
implementation_notes_file,
pretty = TRUE,
auto_unbox = TRUE
)
new_implementation_notes = implementation %>%
dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
comments = purrr::map(lines, ~ {
line_no = which(grepl("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x))
comment = as.list(.x[line_no])
names(comment) = sprintf("line: %d",line_no)
comment
}),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(notes, total, ~ length(.x) / .y * 100)
) %>%
dplyr::select(path, modified, comments, total, percent)
new_implementation_notes = implementation %>%
dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
comments = purrr::map(lines, ~ {
line_no = which(grepl("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x))
comment = as.list(.x[line_no])
names(comment) = sprintf("line: %d",line_no)
comment
}),
total = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(comments, total, ~ length(.x) / .y * 100)
) %>%
dplyr::select(path, modified, comments, total, percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i, ]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive = FALSE)
})
names(tmp) = new_implementation_notes$path
current_implementation_notes = modifyList(implementation_notes, tmp)
current_implementation_notes = current_implementation_notes[order(names(current_implementation_notes))]
jsonlite::write_json(
current_implementation_notes,
implementation_notes_file,
pretty = TRUE,
auto_unbox = TRUE
)
current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 && .x$percent < 25
)
current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 && .x$percent < 25
)
dplyr::as_tibble(current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 && .x$percent < 25
))
current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 && .x$percent < 25
) %>% purrr::imap(~ dplyr::tibble(path = .x, percent = .y$percent))
?purrr::imap
current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 && .x$percent < 25
) %>% purrr::imap(~ dplyr::tibble(path = .y, percent = .x$percent))
dplyr::bind_rows(current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 && .x$percent < 25
) %>% purrr::imap(~ dplyr::tibble(path = .y, percent = .x$percent)))
.write_count(
"[Advisory] Implementation files with comments < 10% or > 25%",
dplyr::bind_rows(current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 && .x$percent < 25
) %>% purrr::imap(~ dplyr::tibble(path = .y, percent = .x$percent)))
)
.write_count = function(title, df) {
write(sprintf("\n## %s: %d", title, nrow(df)), conn)
if (nrow(df) > 0) {
readr::write_tsv(df, conn)
}
# Implementation files 10-25% EM comments
.write_count(
"[Advisory] Implementation files with comments < 10% or > 25%",
dplyr::bind_rows(current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 && .x$percent < 25
) %>% purrr::imap(~ dplyr::tibble(path = .y, percent = .x$percent)))
)
# Implementation files 10-25% EM comments
.write_count(
"[Advisory] Implementation files with comments < 10% or > 25%",
dplyr::bind_rows(current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 && .x$percent < 25
) %>% purrr::imap(~ dplyr::tibble(path = .y, percent = .x$percent)))
)
# Implementation files not linked to designs, test-scripts or issues
.write_count(
"[Advisory] Implementation files with no links to design, test-scripts or issues",
implementation %>%
dplyr::anti_join(
current_links %>% dplyr::filter(type %in% c("IMPLEMENTS", "REPRODUCES")),
by = c("path" = "source")
)
.write_count(
"[Advisory] Implementation files with comments < 10% or > 25%",
dplyr::bind_rows(current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 & .x$percent < 25
) %>% purrr::imap(~ dplyr::tibble(path = .y, percent = .x$percent)))
)
current_implementation_notes
current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 & .x$percent < 25
)
current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 & .x$percent < 25
) %>% purrr::imap(~ dplyr::tibble(path = .y, percent = .x$percent))
dplyr::bind_rows(current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 & .x$percent < 25
) %>% purrr::imap(~ dplyr::tibble(path = .y, percent = .x$percent)))
.write_count(
"[Advisory] Implementation files with no links to design, test-scripts or issues",
implementation %>%
dplyr::anti_join(
current_links %>% dplyr::filter(type %in% c("IMPLEMENTS", "REPRODUCES")),
by = c("path" = "source")
)
.write_count = function(title, df) {
write(sprintf("\n## %s: %d", title, nrow(df)), conn)
if (nrow(df) > 0) {
readr::write_tsv(df, conn, progress = FALSE)
}
.write_count(
"[Advisory] Implementation files with comments < 10% or > 25%",
dplyr::bind_rows(current_implementation_notes %>% purrr::discard(
~ .x$percent > 10 & .x$percent < 25
) %>% purrr::imap(~ dplyr::tibble(path = .y, percent = .x$percent)))
)
implementation_notes = list()
new_implementation_notes = implementation %>%
dplyr::mutate(
lines = purrr::map(path, ~ readr::read_lines(fs::path(dir, .x))),
comments = purrr::map(lines, ~ {
line_no = which(grepl("^\\s*(?:\\/\\/|#['#]?|--|/\\*|%|<!--)\\s*EM:", .x))
comment = as.list(.x[line_no])
names(comment) = sprintf("line: %d", line_no)
comment
}),
count_lines = purrr::map_int(lines, ~ length(.x)),
percent = purrr::map2_dbl(comments, count_lines, ~ length(.x) / .y * 100)
) %>%
dplyr::select(path, modified, comments, count_lines, percent)
tmp = lapply(seq_len(nrow(new_implementation_notes)), function(i) {
row = new_implementation_notes[i, ]
ls = as.list(row %>% dplyr::select(-path))
ls = lapply(ls, unlist, recursive = FALSE)
})
names(tmp) = new_implementation_notes$path
current_implementation_notes = modifyList(implementation_notes, tmp)
current_implementation_notes = current_implementation_notes[order(names(current_implementation_notes))]
jsonlite::write_json(
current_implementation_notes,
implementation_notes_file,
pretty = TRUE,
auto_unbox = TRUE
)
pkgtools::what_has_changed()
